pipeline {
    agent {label 'linux' }
    stages {
	stage('build') {
            steps {
		sh 'echo "runing on a linux agent with label linux" '
		sh 'uname -a' //check system information 
   	     }
	 }
     }
}
